name: Test

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  riscv-tests:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install verilator radare2
          sudo pip install cocotb fusesoc
      - name: Test
        run: |
          git submodule update --init --depth 1
          sbt run
          fusesoc init -y
          ./test.sh
